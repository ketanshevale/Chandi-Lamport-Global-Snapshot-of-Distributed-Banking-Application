Project 3
Ketan Shevale
B00599170

Compilation
1. Unzip tar ball
2. Use make command to compile source code

Execution
1. Run branch.sh branchname portnumber 
separate on each terminal (command line argument) arguments as in branches.txt
If branches.txt have 4 branch, run branch.sh for all 4 branch
2. Run controller.sh initialbalance filename
in another terminal where branches.txt should be in current folder.

Input/Output
At branch level we can see transfered and received balance as well as marker message
at controller we can see the snapshot calling method